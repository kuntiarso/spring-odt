<configuration scan="true">
    <!-- JSON Appender (structured logs for Loki/ELK/Grafana) -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <!-- Standard fields -->
                <timestamp/>
                <logLevel/>
                <threadName/>
                <loggerName/>
                <message/>
                <!-- Include MDC so OpenTelemetry's trace_id/span_id go in the log body -->
                <mdc/>
                <!-- Optional: Spring context info -->
                <context/>
                <!-- Optional: structured arguments -->
                <arguments/>
            </providers>
        </encoder>
    </appender>
    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>
</configuration>